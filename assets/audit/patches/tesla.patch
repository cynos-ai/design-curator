--- a/design-md/tesla/DESIGN.md
+++ b/design-md/tesla/DESIGN.md
@@ -1,3 +1,93 @@
+---
+# Curator supplement, not official brand tokens.
+# Source notes: design-package attachments/sources/tesla.json; Skill assets/source-notes/tesla.json.
+# Partial tokens only: read the complete original prose and source notes.
+version: alpha
+name: Tesla-Inspired-design-analysis
+description: 'Tesla''s website is an exercise in radical subtraction — a digital showroom where the product is everything
+  and the interface is almost nothing. The page opens with a full-viewport hero that fills the entire screen with
+  cinematic car photography: three vehicles arranged on polished concrete against a hazy cityscape sky, with a single
+  model name floating above in translucent white type. There are no decorative borders, no gradients, no patterns,
+  no shadows. The UI exists only to provide just enough navigational structure to get out of the way. Every pixel
+  that isn''t product imagery is white space, and that restraint is the design system''s most powerful statement.'
+colors:
+  primary: '#3E6AE1'
+  on-primary: '#FFFFFF'
+  canvas: '#FFFFFF'
+  canvas-soft: '#F4F4F4'
+  ink: '#171A20'
+  body: '#393C41'
+  muted: '#5C5E62'
+  placeholder: '#8E8E8E'
+  hairline: '#EEEEEE'
+  hairline-strong: '#D0D1D2'
+  nav-frost: rgba(255,255,255,0.75)
+typography:
+  hero-title:
+    fontFamily: Universal Sans Display
+    fontSize: 40px
+    fontWeight: 500
+    lineHeight: 48px
+  product-name:
+    fontFamily: Universal Sans Text
+    fontSize: 17px
+    fontWeight: 500
+    lineHeight: 20px
+  nav-item:
+    fontFamily: Universal Sans Text
+    fontSize: 14px
+    fontWeight: 500
+    lineHeight: 16.8px
+  body-text:
+    fontFamily: Universal Sans Text
+    fontSize: 14px
+    fontWeight: 400
+    lineHeight: 20px
+  button-label:
+    fontFamily: Universal Sans Text
+    fontSize: 14px
+    fontWeight: 500
+    lineHeight: 16.8px
+  sub-link:
+    fontFamily: Universal Sans Text
+    fontSize: 14px
+    fontWeight: 400
+    lineHeight: 20px
+  promo-text:
+    fontFamily: Universal Sans Text
+    fontSize: 22px
+    fontWeight: 400
+    lineHeight: 20px
+  category-label:
+    fontFamily: Universal Sans Text
+    fontWeight: 500
+rounded:
+  px-0: 0px
+  px-4: 4px
+spacing:
+  px-4: 4px
+  px-8: 8px
+  px-16: 16px
+  px-21-44: 21.44px
+components:
+  button-primary:
+    backgroundColor: '{colors.primary}'
+    textColor: '{colors.on-primary}'
+    padding: 4px
+    rounded: '{rounded.px-4}'
+    width: 200px
+  button-secondary:
+    backgroundColor: '{colors.canvas}'
+    textColor: '{colors.body}'
+  nav-button:
+    backgroundColor: transparent
+    textColor: '{colors.ink}'
+    padding: 4px 16px
+    rounded: '{rounded.px-4}'
+  text-link:
+    textColor: '{colors.muted}'
+---
+
 # Design System Inspired by Tesla
 
 ## 1. Visual Theme & Atmosphere
