--- a/design-md/spotify/DESIGN.md
+++ b/design-md/spotify/DESIGN.md
@@ -1,3 +1,141 @@
+---
+# Curator supplement, not official brand tokens.
+# Source notes: design-package attachments/sources/spotify.json; Skill assets/source-notes/spotify.json.
+# Partial tokens only: read the complete original prose and source notes.
+version: alpha
+name: Spotify-Inspired-design-analysis
+description: Spotify's web interface is a dark, immersive music player that wraps listeners in a near-black cocoon
+  (`#121212`, `#181818`, `#1f1f1f`) where album art and content become the primary source of color. The design philosophy
+  is "content-first darkness" — the UI recedes into shadow so that music, podcasts, and playlists can glow. Every
+  surface is a shade of charcoal, creating a theater-like environment where the only true color comes from the iconic
+  Spotify Green (`#1ed760`) and the album artwork itself.
+colors:
+  primary: '#1ed760'
+  canvas: '#121212'
+  surface: '#181818'
+  surface-interactive: '#1f1f1f'
+  surface-card: '#252525'
+  surface-card-alt: '#272727'
+  ink: '#ffffff'
+  body-secondary: '#b3b3b3'
+  text-near-white: '#cbcbcb'
+  text-emphasis: '#fdfdfd'
+  error: '#f3727f'
+  warning: '#ffa42b'
+  info: '#539df5'
+  hairline: '#4d4d4d'
+  hairline-light: '#7c7c7c'
+  surface-light: '#eeeeee'
+  primary-border: '#1db954'
+typography:
+  section-title:
+    fontFamily: SpotifyMixUITitle
+    fontSize: 24px
+    fontWeight: 700
+  feature-heading:
+    fontFamily: SpotifyMixUI
+    fontSize: 18px
+    fontWeight: 600
+    lineHeight: 1.3
+  body-bold:
+    fontFamily: SpotifyMixUI
+    fontSize: 16px
+    fontWeight: 700
+  body:
+    fontFamily: SpotifyMixUI
+    fontSize: 16px
+    fontWeight: 400
+  button-uppercase:
+    fontFamily: SpotifyMixUI
+    fontSize: 14px
+    lineHeight: 1
+  button:
+    fontFamily: SpotifyMixUI
+    fontSize: 14px
+    fontWeight: 700
+    letterSpacing: 0.14px
+  nav-link-bold:
+    fontFamily: SpotifyMixUI
+    fontSize: 14px
+    fontWeight: 700
+  nav-link:
+    fontFamily: SpotifyMixUI
+    fontSize: 14px
+    fontWeight: 400
+  caption-bold:
+    fontFamily: SpotifyMixUI
+    fontSize: 14px
+    fontWeight: 700
+  caption:
+    fontFamily: SpotifyMixUI
+    fontSize: 14px
+    fontWeight: 400
+  small-bold:
+    fontFamily: SpotifyMixUI
+    fontSize: 12px
+    fontWeight: 700
+    lineHeight: 1.5
+  small:
+    fontFamily: SpotifyMixUI
+    fontSize: 12px
+    fontWeight: 400
+  badge:
+    fontFamily: SpotifyMixUI
+    fontSize: 10.5px
+    fontWeight: 600
+    lineHeight: 1.33
+  micro:
+    fontFamily: SpotifyMixUI
+    fontSize: 10px
+    fontWeight: 400
+rounded:
+  px-2: 2px
+  px-4: 4px
+  px-6: 6px
+  px-8: 8px
+  px-100: 100px
+  px-500: 500px
+  px-9999: 9999px
+spacing:
+  px-1: 1px
+  px-2: 2px
+  px-3: 3px
+  px-4: 4px
+  px-5: 5px
+  px-6: 6px
+  px-8: 8px
+  px-10: 10px
+  px-12: 12px
+  px-14: 14px
+  px-15: 15px
+  px-16: 16px
+  px-20: 20px
+components:
+  button-dark-large:
+    backgroundColor: '{colors.surface}'
+    textColor: '{colors.ink}'
+    padding: 0px 43px
+    rounded: '{rounded.px-500}'
+  button-light:
+    backgroundColor: '{colors.surface-light}'
+    textColor: '{colors.surface}'
+    rounded: '{rounded.px-500}'
+  button-outline:
+    backgroundColor: transparent
+    textColor: '{colors.ink}'
+    padding: 4px 16px 4px 36px
+    rounded: '{rounded.px-9999}'
+  play-dark:
+    backgroundColor: '{colors.surface-interactive}'
+    textColor: '{colors.ink}'
+    padding: 12px
+  search-input:
+    backgroundColor: '{colors.surface-interactive}'
+    textColor: '{colors.ink}'
+    padding: 12px 96px 12px 48px
+    rounded: '{rounded.px-500}'
+---
+
 # Design System Inspired by Spotify
 
 ## 1. Visual Theme & Atmosphere
